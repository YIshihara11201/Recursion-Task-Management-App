import React from "react";

const Header = () => {
    return(
        <div className={"row"}>
            <p className={"page-header"}>Task Management App</p>
        </div>
    );
};

export default Header;